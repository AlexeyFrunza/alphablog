<h1 class="text-center purple_text m-3">Articles listing page</h1>
<% if logged_in? %>
  <p class="text-center"><%= link_to 'Create new article', new_article_path, class: "btn purple_btn btn-lg"%></p>   
<% end %>
<div class="flickr_pagination">
  <%= will_paginate @articles, :container => false %>
</div> 
<%= render 'article' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>  
